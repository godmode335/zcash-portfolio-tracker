# Task 6 Report — Unique Site Design System

## Status: DONE

## Steps completed

### Step 1 — Install self-hosted fonts
`@fontsource/space-grotesk` and `@fontsource/inter` installed via npm.

Confirmed in `website/package.json`:
- `"@fontsource/inter": "^5.2.8"`
- `"@fontsource/space-grotesk": "^5.2.10"`

### Step 2 — Font imports in BaseLayout.astro
Added four fontsource CSS imports directly below the existing `global.css` import in the frontmatter of `website/src/layouts/BaseLayout.astro`.

### Step 3 — Overwrite global.css
Replaced the old green/dark system-font CSS with the new warm graphite + gold design system (Zcash `#F4B728`, `--bg: #17130c`, `--gold: #f4b728`, bento grid, pill buttons, Space Grotesk + Inter, left-aligned two-column hero).

### Step 4 — DownloadButton.astro sub-label
Changed `.dl-sub` text from `Free · Windows 10/11 · no account` to `Free · Windows 10/11 · private`.

### Step 5 — Overwrite index.astro
Replaced centered single-column hero + card grid with left-aligned two-column hero + bento grid layout per the task spec. Uses `tiles` array mapped to `.bento .tile` elements with `wide`/`tall` classes. FAQ and bottom CTA sections retained.

### Step 6 — Verification

#### npm run check
```
Result (17 files):
- 0 errors
- 0 warnings
- 1 hint (pre-existing: is:inline script hint in BaseLayout)
```

#### npm run build
```
[build] 10 page(s) built in 1.90s
[build] Complete!
```

Build succeeded with 0 errors.

### Step 7 — Commit
```
[feature/mvp 1200851] feat(web): unique gold/graphite design system (bento, left hero, pill, fonts)
 6 files changed, 112 insertions(+), 145 deletions(-)
```

Committed files:
- `website/package.json` (fontsource deps added)
- `website/package-lock.json` (lockfile updated)
- `website/src/components/DownloadButton.astro`
- `website/src/layouts/BaseLayout.astro`
- `website/src/pages/index.astro`
- `website/src/styles/global.css`

`node_modules/` not committed (gitignored).

## Concerns
None. The `/screenshots/dashboard.png` image will 404 in preview until Task 7 (expected per spec).
