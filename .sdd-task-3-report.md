# Task 3 Report: Simplify Frontend to Single-Coin (ZEC)

## Status: DONE

## Commit
Short-hash: `36117eb`
Message: `feat: single-coin (ZEC) app frontend`
Branch: `feature/mvp`

## Edits Made

### `src/forms.ts`
1. **Import line**: Removed `listCoins` and `type CoinMeta` → `import { addTransaction, createPortfolio } from "./api";`
2. **Fieldset markup**: Removed `<select id="f-coin"></select>`, changed legend to `Add ZEC transaction`, changed `placeholder="Quantity"` to `placeholder="ZEC amount"`.
3. **Deleted block**: Removed `listCoins().then((coins: CoinMeta[]) => { ... })` block that populated `#f-coin`.
4. **Click handler**: Replaced `const coin = (panel.querySelector("#f-coin") as HTMLSelectElement).value;` with `const coin = "zcash";`

### `src/main.ts`
1. **Import line**: Removed `listCoins` from the import → `import { listPortfolios, createPortfolio, getDashboard, refreshPrices, getPortfolioHistory, getCoinHistory } from "./api";`
2. **`app.innerHTML` charts section**: Replaced `<div><select id="coin-chart-select"></select><canvas id="coin-chart" height="120"></canvas></div>` with `<div><h3>ZEC price (30d)</h3><canvas id="coin-chart" height="120"></canvas></div>`
3. **Removed**: `const coinSel = document.getElementById("coin-chart-select") as HTMLSelectElement;`
4. **`show()` function**: Removed the `if (!coinSel.dataset.filled) { ... }` block (which populated coinSel and added change listener). Kept `await drawCoin()`.
5. **`drawCoin()` function**: Replaced dynamic `coinSel.value` references with hardcoded `"zcash"`:
   ```ts
   async function drawCoin() {
     const cc = document.getElementById("coin-chart") as HTMLCanvasElement;
     renderCoinChart(cc, "zcash", await getCoinHistory("zcash", 30));
   }
   ```

## Type Check Result
`npx tsc --noEmit` (via `node_modules\.bin\tsc --noEmit`): **0 errors, 0 warnings** (no output = clean pass).

Note: `npm install` was required first as `node_modules` was absent. Dependencies installed successfully.

## Concerns
None. All changes are minimal and correct. Line-ending warnings from git (LF→CRLF) are cosmetic only.
